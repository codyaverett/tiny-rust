[package]
name = "tiny-random"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = { version = "0.2", default-features = false }

[package.metadata.tiny]
class  = "userland-tools"
build  = "hosted"
domain = "crypto-encoding"
