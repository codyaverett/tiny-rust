[package]
name = "tiny-transformer"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = { version = "0.2", default-features = false }

[package.metadata.tiny]
class  = "compute-ai"
build  = "hosted"
domain = "ai-models"
series = "ai-from-scratch"
